[project]
name = "hal-9000"
version = "2001"
description = "Opinionated AI coding agent and dev environment automation for macOS"
authors = [{ name = "Vinta Chen", email = "vinta.chen@gmail.com" }]
readme = "README.md"
license = "MIT"
requires-python = ">=3.14"
dependencies = []

[project.urls]
Homepage = "https://vinta.ws/code/"
Repository = "https://github.com/vinta/hal-9000"

[dependency-groups]
test = ["pytest>=9.1.1"]
lint = ["ansible-lint>=26.6.0", "ruff>=0.16.0", "ty>=0.0.64", "vermin>=1.8.0"]
audit = ["detect-secrets>=1.5.0", "pre-commit>=4.6.1"]
dev = [
    { include-group = "test" },
    { include-group = "lint" },
    { include-group = "audit" },
    "argcomplete>=3.7.0",
]

[tool.uv]
exclude-newer = "3 days"
no-build = true

[tool.uv.pip]
only-binary = [":all:"]

[tool.ruff]
line-length = 200

# Everything under these directories ships to other machines and runs under whatever `python3` they have, which on a stock macOS is 3.9
[tool.ruff.per-file-target-version]
"bin/**/*.py" = "py39"
"dotfiles/**/*.py" = "py39"
"plugins/**/*.py" = "py39"
"skills/**/*.py" = "py39"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "D",      # pydocstyle
    "COM812", # trailing comma (formatter conflict)
    "CPY001", # copyright notice (license lives in LICENSE, not file headers)
    "T20",    # print statements (CLI tools)
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = [
    "ANN",     # type annotations not needed in tests
    "S101",    # assert is the standard for pytest
    "SLF001",  # testing private methods is intentional
    "PLR2004", # magic values are clear in test assertions
]

[tool.ruff.format]
docstring-code-format = true

[tool.ty.rules]
blanket-ignore-comment = "error"
division-by-zero = "error"
missing-type-argument = "error"
possibly-missing-attribute = "error"
possibly-missing-import = "error"
possibly-unresolved-reference = "error"
unused-ignore-comment = "error"
